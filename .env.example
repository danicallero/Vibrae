# Vibrae - Root .env example

# Public domain used by nginx and Cloudflare (e.g., garden.example.com)
DOMAIN=your-domain.example

# Frontend static server
FRONTEND_PORT=9081
# Path to exported web build, relative to repo root (keep the leading slash)
FRONTEND_DIST=/front/dist

# BACKEND

# Backend API
BACKEND_PORT=8000
BACKEND_MODULE=backend.main:app

# Backend app config
MUSIC_DIR=music
SECRET_KEY=change-me-please
LOG_LEVEL=INFO
# Log rotation
LOG_KEEP=5
LOG_ROTATE_INTERVAL_HOURS=12

# Nginx config file path
NGINX_CONF=nginx.conf

# Cloudflare Tunnel token (required for public HTTPS access)
CLOUDFLARE_TUNNEL_TOKEN=your-token-here